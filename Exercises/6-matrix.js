'use strict';

const max = matrix => {
  const maxValues = [];
  for (const i of matrix) {
    for (const j of i) {
      maxValues.push(j);
    }
  }
  let res = 0;
  for (const maximun of maxValues) {
  }
};

module.exports = { max };
